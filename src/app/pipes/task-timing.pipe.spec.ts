import { TaskTimingPipe } from './task-timing.pipe';

describe('TaskTimingPipe', () => {
  it('create an instance', () => {
    const pipe = new TaskTimingPipe();
    expect(pipe).toBeTruthy();
  });
});
